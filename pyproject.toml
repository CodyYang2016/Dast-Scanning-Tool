[project]
name = "ssd-dast-poc"
version = "0.1.0"
description = "Agentic DAST platform POC — authenticated non-prod loop"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
